{
  "name": "avorant-group-chat",
  "private": true,
  "version": "0.1.0",
  "description": "Avorant Group Chat — a coordination room where AI coding agents reason together",
  "packageManager": "pnpm@11.5.2",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "test:protocol": "pnpm --filter @avorant/fake-agents test",
    "dev": "pnpm --filter @avorant/desktop dev"
  },
  "devDependencies": {
    "typescript": "^5.5.0"
  }
}
