{
  "name": "@avorant/hooks",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "esbuild src/main.ts --bundle --platform=node --target=node18 --format=cjs --outfile=dist/avorant-hook.cjs",
    "test": "pnpm build && vitest run --passWithNoTests"
  },
  "devDependencies": {
    "@avorant/server-core": "workspace:*",
    "@avorant/shared": "workspace:*",
    "@types/node": "^22.0.0",
    "esbuild": "^0.24.0",
    "typescript": "^5.5.0",
    "vitest": "^3.0.0"
  }
}
